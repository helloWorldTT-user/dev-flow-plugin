# Dev-Flow 插件安装指南

## 简介

Dev-Flow 是一个 Claude Code 插件，提供 OpenSpec + Superpowers + Code-Review 联合开发流程编排。
支持三种模式：完整模式（13 步）、排查模式（8 步）、快速模式（6 步）。

## 前置条件

- 已安装 [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code)（v1.0+）
- 已安装以下依赖插件/技能（dev-flow 内部会调用这些命令）：
  - `openspec`（`/opsx:explore`、`/opsx:new`、`/opsx:ff`、`/opsx:verify`、`/opsx:archive`）
  - `superpowers`（`/superpowers:brainstorm`、`/superpowers:write-plan`、`/superpowers:execute-plan`、`/superpowers:verification-before-completion`）
  - `code-review`（`/code-review:code-review`）

> **提示**：如果缺少某个依赖插件，dev-flow 仍可加载，只是在执行对应步骤时会提示缺失。

## 路径约定

本指南用 `<CLAUDE_DIR>` 表示 Claude Code 配置目录：

| 系统 | 路径 |
|------|------|
| macOS / Linux | `~/.claude` |
| Windows | `C:\Users\<你的用户名>\.claude` |

后续步骤中的路径请按实际系统替换。本指南以 macOS 为例。

## 安装方式

### 方式一：从 GitHub Marketplace 安装（推荐）

**第 1 步：注册 Marketplace**

编辑 `<CLAUDE_DIR>/plugins/known_marketplaces.json`。如果文件不存在，手动创建，初始内容为 `{}`。

在 JSON 对象中添加以下条目：

```json
"dev-flow-marketplace": {
  "source": {
    "source": "github",
    "repo": "helloWorldTT-user/dev-flow-plugin"
  },
  "installLocation": "<CLAUDE_DIR>/plugins/marketplaces/dev-flow-marketplace",
  "lastUpdated": "2026-05-22T00:00:00.000Z"
}
```

**第 2 步：重启 Claude Code**

关闭当前会话，重新启动 Claude Code，让 marketplace 注册生效。

**第 3 步：注册插件**

编辑 `<CLAUDE_DIR>/plugins/installed_plugins.json`，在 `"plugins": {` 对象中添加：

```json
"dev-flow@dev-flow-marketplace": [
  {
    "scope": "user",
    "installPath": "<CLAUDE_DIR>/plugins/marketplaces/dev-flow-marketplace/dev-flow",
    "version": "1.0.0",
    "installedAt": "2026-05-22T00:00:00.000Z",
    "lastUpdated": "2026-05-22T00:00:00.000Z"
  }
]
```

> **注意**：`installPath` 必须指向包含 `.claude-plugin/plugin.json` 的 `dev-flow` 目录。

**第 4 步：重启 Claude Code 并验证**

```
/dev-flow 测试一下
```

---

### 方式二：本地目录安装

适用于离线环境或开发调试。

**第 1 步：复制文件**

```bash
# 创建目标目录
mkdir -p ~/.claude/local-marketplace

# 克隆或复制项目
git clone https://github.com/helloWorldTT-user/dev-flow-plugin.git /tmp/dev-flow-plugin
cp -r /tmp/dev-flow-plugin/.claude-plugin ~/.claude/local-marketplace/
cp -r /tmp/dev-flow-plugin/marketplace.json ~/.claude/local-marketplace/
cp -r /tmp/dev-flow-plugin/dev-flow ~/.claude/local-marketplace/
```

复制后的目标结构：

```
~/.claude/local-marketplace/
├── .claude-plugin/
│   └── marketplace.json          ← Marketplace 索引
├── marketplace.json               ← 兼容索引
└── dev-flow/
    ├── .claude-plugin/
    │   └── plugin.json            ← 插件元数据
    ├── agents/
    │   └── dev-flow-driver.md     ← Agent 定义
    └── commands/
        └── dev-flow.md            ← /dev-flow 命令
```

**第 2 步：注册 Marketplace**

编辑 `<CLAUDE_DIR>/plugins/known_marketplaces.json`，添加：

```json
"dev-flow-marketplace": {
  "source": {
    "source": "directory",
    "path": "/Users/<用户名>/.claude/local-marketplace"
  },
  "installLocation": "/Users/<用户名>/.claude/local-marketplace",
  "lastUpdated": "2026-05-22T00:00:00.000Z"
}
```

**第 3 步：注册插件**

编辑 `<CLAUDE_DIR>/plugins/installed_plugins.json`，在 `"plugins": {` 对象中添加：

```json
"dev-flow@dev-flow-marketplace": [
  {
    "scope": "user",
    "installPath": "/Users/<用户名>/.claude/local-marketplace/dev-flow",
    "version": "1.0.0",
    "installedAt": "2026-05-22T00:00:00.000Z",
    "lastUpdated": "2026-05-22T00:00:00.000Z"
  }
]
```

**第 4 步：重启 Claude Code 并验证**

重启会话后运行：
```
/dev-flow 测试一下
```

## 使用方式

安装完成后，在 Claude Code 中使用：

```
/dev-flow 给视频平台加个收藏夹功能
```

或直接描述需求：
```
/dev-flow 帮我排查登录白屏的问题
```

Dev-Flow 会自动推理意图，选择合适的模式，逐步引导完成开发流程。

## 文件说明

| 文件 | 说明 |
|------|------|
| `.claude-plugin/marketplace.json` | Marketplace 索引文件（Claude Code 标准） |
| `marketplace.json` | 兼容索引文件 |
| `dev-flow/.claude-plugin/plugin.json` | 插件元数据（名称、版本、作者） |
| `dev-flow/agents/dev-flow-driver.md` | Agent 定义（自动化触发规则 + 流程步骤） |
| `dev-flow/commands/dev-flow.md` | 命令定义（`/dev-flow` 命令的实现） |

## 卸载

1. 从 `<CLAUDE_DIR>/plugins/installed_plugins.json` 中移除 `dev-flow@dev-flow-marketplace` 条目
2. 从 `<CLAUDE_DIR>/plugins/known_marketplaces.json` 中移除 `dev-flow-marketplace` 条目
3. 删除 `~/.claude/local-marketplace/` 目录（方式二用户）
4. 重启 Claude Code

## 故障排除

| 问题 | 解决方案 |
|------|----------|
| `/dev-flow` 命令不可用 | 检查 `installed_plugins.json` 中 `installPath` 是否正确指向 `dev-flow` 目录；确认该目录下有 `.claude-plugin/plugin.json`；重启 Claude Code |
| 重启后插件消失 | 确认 JSON 格式无误（无尾逗号、无注释）；用 `jsonlint` 验证 |
| Agent 不自动触发 | 确认 `dev-flow-driver.md` 在 `agents/` 目录下且文件名未变 |
| 步骤执行报错 "skill not found" | 安装对应的依赖插件（见前置条件） |
| `known_marketplaces.json` 不存在 | 在 `<CLAUDE_DIR>/plugins/` 下手动创建，初始内容为 `{}` |
| JSON 格式报错 | 用 `python3 -m json.tool <文件>` 验证格式；注意尾逗号和路径转义 |
