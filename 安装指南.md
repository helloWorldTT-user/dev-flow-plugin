# Dev-Flow 插件安装指南

## 简介

Dev-Flow 是一个 Claude Code 插件，提供 OpenSpec + Superpowers + Code-Review 联合开发流程编排。
支持三种模式：完整模式（13 步）、排查模式（8 步）、快速模式（6 步）。

## 前置条件

- 已安装 [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code)（v1.0+）
- 已安装以下依赖插件/技能（dev-flow 内部会调用这些命令）：
  - `openspec`（`/opsx:explore`、`/opsx:new`、`/opsx:ff`、`/opsx:verify`、`/opsx:archive`）
  - `superpowers`（`/superpowers:brainstorm`、`/superpowers:write-plan`、`/superpowers:execute-plan`、`/superpowers:verification-before-completion`）
  - `code-review`（`/code-review:code-review`）

> **提示**：如果缺少某个依赖插件，dev-flow 仍可加载，只是在执行对应步骤时会提示缺失。

## 路径约定

本指南用 `<CLAUDE_DIR>` 表示 Claude Code 配置目录：

| 系统 | 路径 |
|------|------|
| macOS / Linux | `~/.claude` |
| Windows | `C:\Users\<你的用户名>\.claude` |

后续步骤中的路径请按实际系统替换。

## 安装方式

### 方式一：作为本地 Marketplace 安装（推荐）

**第 1 步：创建目录并复制文件**

```bash
# macOS / Linux
mkdir -p ~/.claude/local-marketplace

# 将项目文件复制到 local-marketplace
cp -r <本仓库路径>/.claude-plugin ~/.claude/local-marketplace/
cp -r <本仓库路径>/marketplace.json ~/.claude/local-marketplace/
cp -r <本仓库路径>/dev-flow ~/.claude/local-marketplace/
```

复制后的目标结构：

```
~/.claude/local-marketplace/
├── .claude-plugin/
│   └── marketplace.json        ← Marketplace 索引（Claude Code 识别此文件）
├── marketplace.json             ← 兼容旧版索引
└── dev-flow/
    ├── .claude-plugin/
    │   └── plugin.json          ← 插件元数据
    ├── agents/
    │   └── dev-flow-driver.md   ← Agent 定义
    └── commands/
        └── dev-flow.md          ← /dev-flow 命令
```

**第 2 步：注册 Marketplace**

编辑 `<CLAUDE_DIR>/plugins/known_marketplaces.json`。

如果文件不存在，手动创建，初始内容为 `{}`。

在 JSON 对象中添加以下条目（**注意替换路径为你的实际路径**）：

macOS / Linux 示例：
```json
"dev-flow-marketplace": {
  "source": {
    "source": "directory",
    "path": "/Users/<用户名>/.claude/local-marketplace"
  },
  "installLocation": "/Users/<用户名>/.claude/local-marketplace",
  "lastUpdated": "2026-05-22T00:00:00.000Z"
}
```

Windows 示例：
```json
"dev-flow-marketplace": {
  "source": {
    "source": "directory",
    "path": "C:/Users/<用户名>/.claude/local-marketplace"
  },
  "installLocation": "C:/Users/<用户名>/.claude/local-marketplace",
  "lastUpdated": "2026-05-22T00:00:00.000Z"
}
```

> **Windows 注意**：JSON 中反斜杠需要转义为 `\\`，或者直接用正斜杠 `/`。

**第 3 步：重启 Claude Code 并安装插件**

重启 Claude Code 会话（让 marketplace 注册生效），然后运行：

```
/install-plugin dev-flow
```

**第 4 步：验证**

重启 Claude Code 会话，输入以下命令验证是否可用：
```
/dev-flow 测试一下
```

### 方式二：手动路径安装

1. 将 `dev-flow/` 文件夹复制到目标电脑的任意位置。

2. 在 Claude Code 中运行（替换为实际路径）：
   ```
   /install-plugin /path/to/dev-flow
   ```

3. 重启 Claude Code 会话，验证安装。

### 方式三：从 GitHub 安装（需要先推送到 GitHub）

将本仓库推送到 GitHub 后，可以直接通过 URL 安装：

1. 编辑 `<CLAUDE_DIR>/plugins/known_marketplaces.json`，添加：
```json
"dev-flow-marketplace": {
  "source": {
    "source": "github",
    "repo": "<你的GitHub用户名>/dev-flow-plugin"
  },
  "installLocation": "<CLAUDE_DIR>/plugins/marketplaces/dev-flow-marketplace",
  "lastUpdated": "2026-05-22T00:00:00.000Z"
}
```

2. 重启 Claude Code，运行 `/install-plugin dev-flow`。

## 使用方式

安装完成后，在 Claude Code 中使用：

```
/dev-flow 给视频平台加个收藏夹功能
```

或直接描述需求：
```
/dev-flow 帮我排查登录白屏的问题
```

Dev-Flow 会自动推理意图，选择合适的模式，逐步引导完成开发流程。

## 文件说明

| 文件 | 说明 |
|------|------|
| `.claude-plugin/marketplace.json` | Marketplace 索引文件（Claude Code 标准） |
| `marketplace.json` | 兼容索引文件 |
| `dev-flow/.claude-plugin/plugin.json` | 插件元数据（名称、版本、作者） |
| `dev-flow/agents/dev-flow-driver.md` | Agent 定义（自动化触发规则 + 流程步骤） |
| `dev-flow/commands/dev-flow.md` | 命令定义（`/dev-flow` 命令的实现） |

## 卸载

在 Claude Code 中运行：
```
/uninstall-plugin dev-flow
```

然后从 `known_marketplaces.json` 中移除 `dev-flow-marketplace` 条目，并删除 `~/.claude/local-marketplace/` 目录。

## 故障排除

| 问题 | 解决方案 |
|------|----------|
| `/install-plugin dev-flow` 提示找不到 | 检查 `known_marketplaces.json` 中的路径是否正确；确认 `.claude-plugin/marketplace.json` 存在；重启 Claude Code |
| `/dev-flow` 命令不可用 | 确认安装成功后重启会话；检查 `installed_plugins.json` 中是否有 `dev-flow` 条目 |
| Agent 不自动触发 | 确认 `dev-flow-driver.md` 在 `agents/` 目录下且文件名未变 |
| 步骤执行报错 "skill not found" | 安装对应的依赖插件（见前置条件） |
| `known_marketplaces.json` 不存在 | 在 `<CLAUDE_DIR>/plugins/` 下手动创建，初始内容为 `{}` |
| JSON 格式报错 | 用 `jsonlint` 或在线工具验证 JSON 格式；注意尾逗号和转义 |
